"""Framework integrations. Import the one you need directly::

    from agperms.integrations.langgraph import AgpermsGuard

Nothing here is imported by ``agperms`` itself, so an integration's optional
dependency never becomes a dependency of the core.
"""

from __future__ import annotations

__all__: list[str] = []
